a = [55, 65, -95, 55.6, -95]
# pos = a.index(-95)
# print(pos)
# a.sort

# a.reverse()


# to have it in descending order
# a.sort(reverse=true)
# a.append(["Vats", "dumb", "stupid"]) -> It will add the list to another list
# a.extend(["Vats", "dumb", "stupid"]])-> This will add all the element to the original list.
r = a.count(-95)
print(r)
a.clear()
print(a)
